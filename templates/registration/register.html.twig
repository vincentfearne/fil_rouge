{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}


    <section class="container blur w">
        <div class="row">
            <div class="col mt-3">
                <h1 class="w">Inscription</h1>
                {{ form_start(registrationForm) }}
                    <fieldset class="mb-3">
                        <legend>Mon identité</legend>
                        {{ form_row(registrationForm.userfirstname) }}
                        {{ form_row(registrationForm.userlastname) }}
                        {{ form_row(registrationForm.usermail) }}
                    </fieldset>
                {{ form_row(registrationForm.username) }}
                {{ form_row(registrationForm.plainPassword) }}
                {{ form_row(registrationForm.RGPDConsent) }}

                <button type="submit" class="btn btn-primary btn-lg my-3">Valider</button>
                {{ form_end(registrationForm) }}
            </div>
        </div>
    </section>


{% endblock %}
